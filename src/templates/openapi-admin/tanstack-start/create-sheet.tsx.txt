// Generated by fizzyx. Do not edit.

"use client"

import { useState } from "react"
import { {{FIZZYX_HOOK_NAME}} } from "@/lib/api/generated"
import { {{FIZZYX_SCHEMA_NAME}} } from "@/generated/admin/forms/{{FIZZYX_RESOURCE_ID}}"
import { AdminActionSurface } from "@/components/admin/admin-action-surface"
import { ResourceForm } from "@/components/admin/resource-form"
import { Button } from "@/components/ui/button"

export function {{FIZZYX_COMPONENT_NAME}}({ onSaved }: { onSaved: () => Promise<void> }) {
  const [open, setOpen] = useState(false)
  const mutation = {{FIZZYX_HOOK_NAME}}()
  return <AdminActionSurface mode="sheet" title={{FIZZYX_TITLE}} description="Complete the fields below to add a new record." trigger={<Button>New {{FIZZYX_LABEL}}</Button>} open={open} onOpenChange={setOpen}><ResourceForm descriptor={{ schema: {{FIZZYX_SCHEMA_NAME}}, initialValue: {} }} pending={mutation.isPending} error={mutation.error} onSubmit={async (value) => { await mutation.mutateAsync(value as never); await onSaved(); setOpen(false) }} /></AdminActionSurface>
}
