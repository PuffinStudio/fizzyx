// Generated by fizzyx. Do not edit.

import { createFileRoute{{FIZZYX_ROUTER_IMPORTS}} } from "@tanstack/react-router"
{{FIZZYX_DELETE_IMPORTS}}import { Alert, AlertDescription, AlertTitle } from "@/components/ui/alert"
import { Skeleton } from "@/components/ui/skeleton"
import { RecordDetails } from "@/components/admin/record-details"
import { readAdminPath } from "@/components/admin/admin-runtime"
import { {{FIZZYX_HOOK_IMPORTS}} } from "@/lib/api/generated"

export const Route = createFileRoute({{FIZZYX_ROUTE}})({ component: {{FIZZYX_COMPONENT_NAME}} })
const apiIdParam = {{FIZZYX_ID_PARAM}}
const resourceLabel = {{FIZZYX_LABEL}}
const detailPath = {{FIZZYX_DETAIL_PATH}}
function {{FIZZYX_COMPONENT_NAME}}() {
  {{FIZZYX_NAV_DECLARATION}}
  const routeParams = Route.useParams()
  const params = { [apiIdParam]: routeParams.id as never }
  const query = {{FIZZYX_DETAIL_HOOK}}(params as never)
  {{FIZZYX_DELETE_DECLARATION}}
  if (query.isPending) return <div className="space-y-4"><Skeleton className="h-10 w-64" /><Skeleton className="h-80 w-full" /></div>
  if (query.error) return <Alert variant="destructive"><AlertTitle>Unable to load record</AlertTitle><AlertDescription>{query.error.message}</AlertDescription></Alert>
  return <section className="mx-auto max-w-5xl space-y-6"><header className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div className="space-y-1"><p className="text-xs font-semibold uppercase tracking-[0.18em] text-primary">Record Details</p><h1 className="text-3xl font-semibold tracking-tight text-balance">{resourceLabel}</h1><p className="text-sm text-muted-foreground">Review the current API representation for this record.</p></div><div className="flex items-center gap-2"><a className="text-sm font-medium text-muted-foreground underline-offset-4 hover:text-foreground hover:underline" href={{FIZZYX_LIST_ROUTE}}>Back to {resourceLabel}</a>{{FIZZYX_DELETE_BUTTON}}</div></header><RecordDetails data={readAdminPath(query.data, detailPath)} /></section>
}
