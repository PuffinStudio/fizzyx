// Generated by fizzyx. Do not edit.

import { createFileRoute } from "@tanstack/react-router"
import { {{FIZZYX_HOOK_IMPORTS}} } from "@/lib/api/generated"
import { DynamicForm } from "@/components/admin/dynamic-form"

export const Route = createFileRoute({{FIZZYX_ROUTE}})({ component: {{FIZZYX_COMPONENT_NAME}} })
const fields = {{FIZZYX_FIELDS}} as const
const apiIdParam = {{FIZZYX_ID_PARAM}}
const resourceLabel = {{FIZZYX_LABEL}}
function {{FIZZYX_COMPONENT_NAME}}() {
  const routeParams = Route.useParams()
  const params = { [apiIdParam]: routeParams.id as never }
  {{FIZZYX_DETAIL_DECLARATION}}
  const mutation = {{FIZZYX_UPDATE_HOOK}}()
  return <section className="space-y-6"><h1 className="text-2xl font-semibold">Edit {resourceLabel}</h1><DynamicForm fields={fields} initialValue={{{FIZZYX_INITIAL_VALUE}}} pending={mutation.isPending} error={mutation.error} success={mutation.isSuccess} onSubmit={async (value) => { await mutation.mutateAsync({ params, data: value } as never) }} /></section>
}
