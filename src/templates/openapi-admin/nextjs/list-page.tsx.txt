// Generated by fizzyx. Do not edit.

"use client"

{{FIZZYX_REACT_IMPORT}}import { {{FIZZYX_HOOK_NAME}}{{FIZZYX_CREATE_HOOK_IMPORT}} } from "@/lib/api/generated"
{{FIZZYX_NAV_IMPORTS}}{{FIZZYX_CREATE_IMPORT}}import { AdminDataTable{{FIZZYX_TABLE_TYPE_IMPORT}} } from "@/components/admin/data-table"
import { applyAdminColumns, readAdminNumberPath, readAdminPath } from "@/components/admin/admin-runtime"
import { adminConfig } from "@/admin/config"
import { adminRegistries } from "@/admin/registries"

const columns = {{FIZZYX_COLUMNS}} as const
const resolvedColumns = applyAdminColumns(columns, adminConfig.resources?.[{{FIZZYX_RESOURCE_ID}}]?.columns, adminRegistries.cells)
const resourceLabel = {{FIZZYX_LABEL}}
const rowsPath = {{FIZZYX_ROWS_PATH}}
const totalPath = {{FIZZYX_TOTAL_PATH}}
{{FIZZYX_CREATE_FIELDS}}

export default function {{FIZZYX_COMPONENT_NAME}}() {
  {{FIZZYX_STATE_DECLARATION}}
  const query = {{FIZZYX_HOOK_NAME}}{{FIZZYX_HOOK_ARGS}}
  {{FIZZYX_CREATE_DECLARATION}}
  return <section className="space-y-6"><header className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div className="space-y-1"><p className="text-xs font-semibold uppercase tracking-[0.18em] text-primary">Resource</p><h1 className="text-3xl font-semibold tracking-tight text-balance">{resourceLabel}</h1><p className="max-w-2xl text-sm text-muted-foreground text-pretty">Review, search, and manage {resourceLabel.toLowerCase()} from one workspace.</p></div>{{FIZZYX_CREATE_ACTION}}</header><AdminDataTable data={readAdminPath(query.data, rowsPath)} rowCount={readAdminNumberPath(query.data, totalPath)} columns={resolvedColumns} loading={query.isPending} error={query.error} onRetry={() => void query.refetch()}{{FIZZYX_TABLE_PROPS}}{{FIZZYX_ROW_ACTIONS}} /></section>
}
