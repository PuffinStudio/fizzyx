// Generated by fizzyx. Do not edit.

"use client"

import { useParams{{FIZZYX_ROUTER_IMPORTS}} } from "next/navigation"
import { Button } from "@/components/ui/button"
import { {{FIZZYX_HOOK_IMPORTS}} } from "@/lib/api/generated"

const apiIdParam = {{FIZZYX_ID_PARAM}}
const resourceLabel = {{FIZZYX_LABEL}}
export default function {{FIZZYX_COMPONENT_NAME}}() {
  {{FIZZYX_NAV_DECLARATION}}
  const routeParams = useParams<{ id: string }>()
  const params = { [apiIdParam]: routeParams.id as never }
  const query = {{FIZZYX_DETAIL_HOOK}}(params as never)
  {{FIZZYX_DELETE_DECLARATION}}
  if (query.isPending) return <p>Loading…</p>
  if (query.error) return <p className="text-destructive">{query.error.message}</p>
  return <section className="space-y-6"><h1 className="text-2xl font-semibold">{resourceLabel} details</h1><pre className="overflow-auto rounded-md border bg-muted p-4 text-sm">{JSON.stringify(query.data, null, 2)}</pre>{{FIZZYX_DELETE_BUTTON}}</section>
}
