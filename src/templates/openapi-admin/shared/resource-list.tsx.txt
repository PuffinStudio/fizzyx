// Generated by fizzyx. Do not edit.

import type { ReactNode } from "react"
import { DataGrid, type DataGridColumn } from "@/components/admin/data-grid"
import { QueryState } from "@/components/admin/query-state"

export interface ResourceListResult {
  rows: readonly Record<string, unknown>[]
  total?: number
}

export function ResourceList({ title, description, query, columns, actions, renderRowActions }: {
  title: string
  description?: string
  query: { data?: ResourceListResult; loading: boolean; error?: unknown; retry?: () => void }
  columns: readonly DataGridColumn[]
  actions?: ReactNode
  renderRowActions?: (row: Record<string, unknown>) => ReactNode
}) {
  return <section className="space-y-6"><header className="flex flex-wrap items-start justify-between gap-4"><div><h1 className="text-2xl font-semibold tracking-tight">{title}</h1>{description ? <p className="mt-1 text-sm text-muted-foreground">{description}</p> : null}</div>{actions}</header><QueryState data={query.data} loading={query.loading} error={query.error} onRetry={query.retry}>{({ rows, total }) => <DataGrid rows={rows} columns={columns} rowCount={total} renderRowActions={renderRowActions} />}</QueryState></section>
}
